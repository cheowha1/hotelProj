<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="hotelproject.mappers.HotelMapper">

    <!-- 특정 호텔 조회 (phone 추가됨) -->
    <select id="getHotelById" resultType="hotelproject.repositories.vo.HotelVo">
        SELECT hotelNo, name, location, phone, availableRooms, rating 
        FROM hotel 
        WHERE hotelNo = #{hotelNo};
    </select>

    <!-- 모든 호텔 조회 (phone 추가됨) -->
    <select id="getAllHotels" resultType="hotelproject.repositories.vo.HotelVo">
        SELECT hotelNo, name, location, phone, availableRooms, rating 
        FROM hotel;
    </select>

    <!-- 호텔 정보 업데이트 (phone 추가됨) -->
    <update id="updateHotel">
        UPDATE hotel 
        SET name = #{name}, location = #{location}, phone = #{phone}, availableRooms = #{availableRooms}, rating = #{rating}
        WHERE hotelNo = #{hotelNo};
    </update>

</mapper>
