<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="hotelproject.mapper.ReviewMapper">

    <!-- 리뷰 추가 -->
    <insert id="insertReview">
        INSERT INTO reviews (userid, hotel_name, review_text, rating, created_at)
        VALUES (#{userid}, #{hotelName}, #{reviewText}, #{rating}, NOW());
    </insert>

    <!--  호텔의 리뷰 목록 조회 -->
    <select id="getReviewsByHotel" resultType="hotelproject.repositories.vo.ReviewVo">
        SELECT * FROM reviews WHERE hotel_name = #{hotelName} ORDER BY created_at DESC;
    </select>

    <!--  호텔의 평균 별점 조회 -->
    <select id="getAverageRating" resultType="Double">
        SELECT IFNULL(AVG(rating), 0) FROM reviews WHERE hotel_name = #{hotelName};
    </select>

    <!-- 리뷰 수정 -->
    <update id="updateReview">
        UPDATE reviews 
        SET review_text = #{reviewText}, rating = #{rating}, created_at = NOW()
        WHERE userid = #{userid} AND hotel_name = #{hotelName};
    </update>

    <!-- 리뷰 삭제 -->
    <delete id="deleteReview">
        DELETE FROM reviews WHERE userid = #{userid} AND hotel_name = #{hotelName};
    </delete>

</mapper>
