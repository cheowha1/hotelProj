<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="hotelproject.mappers.PointMapper">

    <insert id="insertPointTransaction">
        INSERT INTO points (user_id, amount, transaction_type, created_at)
        VALUES (#{userId}, #{amount}, #{transactionType}, NOW());
    </insert>

    <select id="getPointsByUserId" resultType="hotelproject.repositories.vo.PointVo">
        SELECT SUM(amount) as total_points FROM points WHERE user_id = #{userId};
    </select>

    <update id="updateUserPoints">
        UPDATE users SET point = point + #{amount} WHERE id = #{userId};
    </update>

</mapper>
